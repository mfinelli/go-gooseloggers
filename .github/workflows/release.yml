---
name: Release
on:
  push:
    tags: [v*]

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest
    steps:
      - uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
